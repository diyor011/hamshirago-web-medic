# HamshiraGo — Активные задачи

> Обновляется при каждом изменении. Выполненные задачи переносить в `done.md`.

---

## 🐛 Баги

- [ ] **[mobile]** При завершении заказа медиком у клиента не появляется окно о завершении и поле для оценки — нужно убедиться что Socket.IO событие `order:status` обрабатывается на экране трекинга и триггерит UI оценки (`mobile/app/order/track.tsx`)

- [ ] **[medic]** Завершённые заказы у медика обновляются только после pull-to-refresh — нужно добавить авто-обновление списка при получении Socket.IO события об изменении статуса (`medic/app/(tabs)/index.tsx` или экран "Мои заказы")

- [ ] **[mobile/medic]** При логине с несуществующими данными выводится только `"password or number invalid"` — нужно улучшить обработку ошибок и показывать читаемое сообщение пользователю (`mobile/app/auth.tsx`, `medic/app/auth.tsx`)

---

## 📋 Задачи

- [ ] **[backend]** Добавить `GET /medics/admin/all` — для страницы "Все медики" в админке (сейчас используется `GET /medics/admin/pending`, показывает только PENDING)
- [ ] **[backend]** Добавить `GET /auth/admin/users` — для страницы "Клиенты" в админке (сейчас заглушка)

---

## 💡 Идеи / V1

- [ ] Разделить таблицу `payments` — отдельный `payments_ledger` для прозрачности финансов
- [ ] Helmet + structured logging (pino) для production
- [ ] Push уведомления медику о новых заказах через Telegram Bot — частично реализовано, нужно интегрировать в medic mobile app сохранение `chatId`
- [ ] Аналитика в admin: графики заказов, выручка, топ медики
- [ ] Фильтр услуг по категории на главном экране mobile/web
- [ ] Повторный заказ (кнопка "Заказать снова" в истории)
